<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lalchand.myapplication" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- <uses-library android:name="com.google.android.maps"/> -->

        <activity
            android:name=".MyActivity"
            android:label="@string/app_name" >

            <!-- android:theme="@android:style/Theme.Dialog"> -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DisplayMessageActivity"
            android:label="@string/title_activity_display_message" >

            <!-- android:theme="@android:style/Theme.Dialog">   for dialog appearance -->
            <!--
            <intent-filter>
                <action android:name="net.learn2develop.DisplayMessageActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            -->

        </activity>
        <activity
            android:name=".D3Acitivity"
            android:label="@string/title_activity_d3_acitivity" >
        </activity>
        <!--<activity-->
            <!--android:name=".Network"-->
            <!--android:label="@string/title_activity_network" >-->
        <!--</activity>-->
    </application>

</manifest>
